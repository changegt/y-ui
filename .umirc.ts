import { defineConfig } from "dumi";

export default defineConfig({
  title: "组件库",
  mode: "site",
  // logo: "",
  // more config: https://d.umijs.org/config
  publicPath: "/",
  hash: true,
  history: { type: "hash" },
});